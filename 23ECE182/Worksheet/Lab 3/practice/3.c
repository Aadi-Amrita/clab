#include <stdio.h> 

int main() { 
    int n = 6; 
    if (n >= 10 && n <= 100) { 
        printf("%d lies in range.", n); 
    } 
    else { 
        printf("%d does not lie in range.", n); 
    } 
    return 0; 
} 