#include <stdio.h>

int main(){
    int N=8;
    int sum=0;
    printf("N=%d\n",8);
    sum+=N;
    N--;
    sum+=N;
    N--;
    sum+=N;
    N--;
    sum+=N;
    N--;
    sum+=N;
    N--;
    sum+=N;
    N--;
    sum+=N;
    N--;
    sum+=N;
    printf("sum=%d",sum);
    return 0;
}